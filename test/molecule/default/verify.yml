- hosts: all
  become: yes
  gather_facts: no
  tasks:
    - import_tasks: test_cases/01_libvirt_start.yml
      vars:
        test_name: Initiate guest startup from Libvirt

    - import_tasks: test_fixtures/results.yml
